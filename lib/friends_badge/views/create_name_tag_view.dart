import 'package:flutter/material.dart';

class CreateNameTagView extends StatelessWidget {
  const CreateNameTagView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
